# 串口继电器通讯协议文档

## 1. 概述

本协议用于通过串口控制多路继电器板，实现对继电器通道的通断控制与状态查询。
通讯方式支持**单向控制**（无应答）与**带应答控制**（有返回）两种模式。

## 2. 串口参数

| 参数 | 值 |
|---|---|
| 波特率 | 9600 |
| 数据位 | 8 |
| 停止位 | 1 |
| 校验位 | None（无） |
| 流控 | 无 |

## 3. 帧格式

定长 4 字节帧，结构如下：

```
+--------+--------+--------+-----------+
| Header | Addr   | Func   | Checksum  |
+--------+--------+--------+-----------+
| 1 byte | 1 byte | 1 byte | 1 byte    |
+--------+--------+--------+-----------+
```

### 3.1 字段说明

#### Header（帧头）
- 固定值：`0xA0`
- 用于标识一帧的开始。

#### Addr（地址码）
- 范围：`0x01` ~ `0xFE`
- 用于指定要操作的继电器通道号。
- `0x00` 与 `0xFF` 不作为有效通道使用。

#### Func（功能码）
- 见第 4 节功能码定义。

#### Checksum（校验和）
- 计算方式：`(Header + Addr + Func) & 0xFF`
- 即前三字节之和取低 8 位。

## 4. 功能码定义

| 功能码 | 功能 | 是否返回 | 说明 |
|---|---|---|---|
| `0x00` | 关继电器 | 否 | 使继电器不吸合 |
| `0x01` | 开继电器 | 否 | 使继电器吸合 |
| `0x02` | 关继电器 | 是 | 使继电器不吸合，并返回当前状态 |
| `0x03` | 开继电器 | 是 | 使继电器吸合，并返回当前状态 |
| `0x04` | 取反继电器 | 是 | 翻转当前状态，并返回新状态 |
| `0x05` | 查询状态 | 是 | 不改变状态，仅返回当前状态 |

## 5. 返回帧格式

带返回的指令执行后，继电器板会回传一个定长 4 字节帧，结构如下：

```
+--------+--------+--------+-----------+
| Header | Addr   | State  | Checksum  |
+--------+--------+--------+-----------+
| 1 byte | 1 byte | 1 byte | 1 byte    |
+--------+--------+--------+-----------+
```

### 5.1 字段说明

#### Header（帧头）
- 固定值：`0xA0`

#### Addr（地址码）
- 与发送帧中地址码一致，用于标识返回状态对应的通道号。

#### State（状态码）
- `0x00` → 继电器不吸合
- `0x01` → 继电器吸合

#### Checksum（校验和）
- 计算方式：`(Header + Addr + State) & 0xFF`

## 6. 示例命令

### 6.1 无返回指令

| 操作 | 发送帧（HEX） | 说明 |
|---|---|---|
| 1 号通道关 | `A0 01 00 A1` | Func=0，不吸合 |
| 1 号通道开 | `A0 01 01 A2` | Func=1，吸合 |
| 2 号通道关 | `A0 02 00 A2` | Func=0，不吸合 |
| 2 号通道开 | `A0 02 01 A3` | Func=1，吸合 |

### 6.2 带返回指令

| 操作 | 发送帧（HEX） | 返回帧示例（HEX） | 说明 |
|---|---|---|---|
| 1 号通道关（带返回） | `A0 01 02 A3` | `A0 01 00 A1` | 返回当前状态为不吸合 |
| 1 号通道开（带返回） | `A0 01 03 A4` | `A0 01 01 A2` | 返回当前状态为吸合 |
| 1 号通道取反 | `A0 01 04 A5` | `A0 01 01 A2` | 翻转后返回新状态 |
| 1 号通道查询 | `A0 01 05 A6` | `A0 01 01 A2` | 返回当前状态为吸合 |

### 6.3 校验和验算示例

- 1 号通道查询发送：`(0xA0 + 0x01 + 0x05) & 0xFF = 0xA6` ✓
- 1 号通道吸合状态返回：`(0xA0 + 0x01 + 0x01) & 0xFF = 0xA2` ✓
- 2 号通道开（带返回）发送：`(0xA0 + 0x02 + 0x03) & 0xFF = 0xA5` ✓

## 7. 备注

- 无返回指令（功能码 `0x00`、`0x01`）发送后无需等待应答。
- 带返回指令（功能码 `0x02` ~ `0x05`）发送后应等待应答帧，建议设置合理的超时时间。
- 每次发送控制帧后，建议延时约 20ms 等待继电器机械动作完成。
- 继电器的吸合状态与外部负载的通断状态之间的关系取决于实际接线方式（常开端 NO 或常闭端 NC），本协议仅描述继电器本身的吸合状态。
